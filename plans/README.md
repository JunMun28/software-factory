# Implementation Plans

Generated by the improve skill on 2026-06-11, against commit `76bb314` plus
the then-uncommitted intake value-fields work (reach/impact). Execute in the
order below unless dependencies say otherwise. Each executor: read the plan
fully before starting, honor its STOP conditions, and update your row when
done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fix stale route params (admin issue + channel feed) | P1 | S | — | DONE (advisor-approved after 1 revision; live-verified: issue 13→14 nav shows the new issue) |
| 002 | Unit-test the core web services (poll, session, intake-draft) | P1 | M | — | DONE (advisor-approved 2026-06-11, branch advisor/fix-all) |
| 003 | Lint & format gate (eslint + ruff + prettier, Makefile + CI) | P2 | M | — | DONE (advisor-approved; 6 TODO(lint-debt) rules documented in eslint.config.js / pyproject.toml) |
| 004 | Consolidate dropdowns into one `sf-pop-menu` kit component | P2 | M | 003 (soft) | DONE (advisor-approved; visual spot-check pending merge) |
| 005 | Runner/brain hardening (log truncation, prompt delimiters, ref check, metric fallback) | P2 | S | — | DONE (advisor-approved; one formatting-only out-of-scope commit af24975, verified harmless) |
| 006 | Split main.py into domain routers (behavior-preserving) | P3 | L | 005, 003 | DONE (advisor-approved; route snapshot identical, main.py 115 lines, steps 5+6 committed together to keep every commit green) |
| 007 | AGENTS.md + CLAUDE.md + fix stale doc counts | P3 | S | best last | DONE (advisor-approved) |

All execution on branch `advisor/fix-all` (worktree `../software-factory-fixes`):
29 commits, 62 files, final gate green (make verify incl. lint; 62 backend +
34 web tests; route snapshot identical; operator's checkout untouched).
One extra commit outside the plans: `28976c5` pins the dev proxy to
127.0.0.1 — an unrelated Docker container publishing [::]:8000 was
intercepting `/api` calls via IPv6 localhost resolution.
Merging to main is the operator's decision.

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Intake motion plans (2026-07-21, commit `a963e53`)

Generated by the `improve-animations` skill after a
`find-animation-opportunities` sweep of the intake app (AIRES). The sweep found
the app **already well-animated** — tokens, entrance classes, a `linear()`
spring, per-component keyframes and reduced-motion handling all present. Only
three genuine gaps survived the gate; everything else was either correct by
design or already handled.

| Plan | Title | Severity | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 009 | Staggered entrance for the live plan panel's rewrite | MEDIUM | S | — | DONE (live-verified, uncommitted) |
| 010 | Delight beat on the submission-confirmation check | LOW | S | — | DONE (live-verified, uncommitted) |
| 011 | Fade-in for the plan panel's fact chips | LOW | XS | — | DONE (live-verified, uncommitted) |

**Recommended order: 009 → 011 → 010.** 009 is the only one with real leverage —
the plan panel's rewrite is the app's signature interaction and currently has no
bridge at all. 011 touches the same file, so doing it second is one context load
instead of two (its Boundaries section says how to merge the two reduced-motion
edits). 010 is independent and can be done any time, or dropped.

No hard dependencies. 009 and 011 both edit
`apps/intake/src/app/submitter/plan-panel.ts`; if executed in parallel they will
conflict, so run them sequentially or in one pass.

### Rejected during the sweep (do not re-audit)

- **`.dock__opt` highlight and the `↩` indicator** (`interview.ts:180`) —
  keyboard-driven option navigation, hit dozens of times per session. The
  existing 80ms background transition is already at the right edge; animating
  more would make it feel laggy.
- **Streaming token deltas** (`interview.ts:150`) — functional content the user
  is actively reading. Per-token fades fight comprehension and would make a ~1s
  TTFT feel like 2s.
- **`.sub-nav` buttons and the AIRES wordmark** (`styles.css:610`) — core
  navigation, 100+/day. Correctly hover-only.
- **basics → interview phase swap** (`interview.ts:71`) — *not a gap*: already
  done well, with `iv-in` on the workspace and a 0.12s stagger onto the plan
  panel, reduced-motion handled.
- **Chat rows, escalation banner, request cards** — *not gaps*: already carry
  `.fade-in`.

## Correctness & gate plans (2026-07-21, commit `5b9facb`)

Generated by the `improve` skill from a four-auditor sweep (backend
correctness + perf, ng-v0 orchestrator + tests, security + deps,
architecture/DX/docs/direction). Every finding was re-opened and confirmed
against the code before it became a plan. The operator selected these eight
findings out of the full table; the rest are recorded below so they are not
re-audited.

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 012 | Put `app-preview/` into `task verify` and CI | P1 | S | — | DONE (advisor-approved; merged to local main `7645d10`) |
| 013 | Stop leaking sandbox pods (await teardown, guard start-vs-stop, reap orphans) | P1 | M | 012 (soft) | DONE (advisor-approved; 6 commits, merged to local main `e39e504`) |
| 014 | Make "restore version" change the live cloud preview | P2 | S | 012 (soft) | DONE (advisor-approved 2026-07-21; merged to local main `3b16094`) |
| 015 | Serialize PlatformDb transactions | P2 | M | 012 (soft) | DONE (advisor-approved 2026-07-21; merged to local main `a487e33`) |
| 016 | Agent-facing docs truth (`task verify`, dead `web/` paths) | P2 | S | — | DONE (advisor-approved 2026-07-22; merged to local main `5fa6c18`) |
| 017 | Address requester emails to an email, not a display name | P2 | S | — | DONE (advisor-approved 2026-07-22 after 1 revision; branch `advisor/017-requester-email`, 2 commits, NOT merged — blocked, see record) |
| 018 | Close two import-edit races (shared temp ref, crash window) | P2 | M | — | TODO |
| 019 | Pre-push secret hygiene (template runbook identifiers, de-hardcode the CI password) | P1 | S+M | — | DONE (advisor-approved 2026-07-22; merged to local main `28dd78b`) |

**Recommended order: 012 → 013 → 014 → 015 → 016 → 017 → 018.**

**012 execution record (2026-07-21).** Executed by a subagent in an isolated
worktree, reviewed and independently re-verified by the advisor. Diff is two
files only (`Taskfile.yml`, `.github/workflows/ci.yml`); nothing under
`app-preview/` was touched and neither `package-lock.json` changed. Verified:
`task verify` exit 0 with `✓ VERIFY PASSED`, and the executed recipe order
confirms `appview-typecheck` + `appview-test` run between `test-web` and
`build`. Suites now gated that previously ran nowhere — orchestrator **24
files / 188 tests**, app-preview UI **45 files / 209 tests** (397 total). Both
were already green, so this was a pure gate addition; the plan's MED risk did
not materialise. The UI runs on `@angular/build:unit-test` (vitest + jsdom),
headless — so the CI job needs no browser. Merged to local main (fast-forward, `7645d10`). If branch protection
lists required checks, add `verify-appview` to it.

**013 execution record (2026-07-21).** Executed in an isolated worktree,
reviewed and independently re-verified by the advisor. 6 conventional commits
on `advisor/013-sandbox-lifecycle`. Verified: `npm test` → **24 files / 195
tests** (188 baseline + 7 new), `npm run typecheck` clean, and every grep
criterion holds (`isFailed` → 0 occurrences, no fire-and-forget
`record.sandbox?.stop()`, `sf/tier` now read in 3 places, `reapOrphans` called
at boot). The 4 lifecycle tests were confirmed to FAIL before their fix and
PASS after; the 3 reaper tests cover new code, so the executor proved them
non-vacuous by temporarily stubbing `reapOrphans` to a no-op and to the naive
`new Set(knownChatIds)` bug, watching them fail, then restoring byte-for-byte.
Two review notes: the `as never` casts in the new tests match a pre-existing
convention in that same file (original line 88), not new debt; and
`implementation-notes.md` was edited outside the plan's in-scope list —
accepted on merit, because the operator's standing rule requires deviations to
be logged there, and it is documentation with no behavioural effect.
**Plan bug found by the executor**: plan 013's suggested doc-comment for
`startToken` contained the literal `isFailed`, which contradicted its own
`grep -c "isFailed" → 0` done criterion. It resolved conservatively (reworded
to "the old failed-only guard") and logged it. Left unmerged; merging is the
operator's call.

**014 execution record (2026-07-21).** Smallest of the batch and clean: one
5-line change in `restoreVersion` plus a 28-line test, nothing else. Verified by
the advisor: `npm test` → **24 files / 196 tests**, typecheck clean,
`onVersionCreated` now has exactly 3 occurrences in `chat-store.ts`. The test
asserts the sha equals the **restore** commit (not the source version's), which
is the assertion that matters — and I proved it non-vacuous by deleting the one
fix line and watching it fail with `expected [] to have a length of 1`. The
executor also checked the other `insertVersion` call sites for the same
omission and correctly found none that need it (rehydrate runs at boot with no
live sandbox; `forkVersion` makes a new chat). Merged to local main
(fast-forward, `3b16094`).

**015 execution record (2026-07-21).** The riskiest of the batch — it rewrites
the write path for every persisted turn event — and it came back clean.
Verified by the advisor: `npm test` → **24 files / 200 tests**, typecheck clean,
`begin`/`commit`/`rollback` gone from the `SqlDriver` interface (confirmed by
parsing the interface body, not just grepping), 7 `withTransaction` sites in
`platform-db.ts`, and zero raw `driver.begin()` calls left. I ran my OWN
bypass probe — stripped the serialization gate out of
`SqliteDriver.withTransaction` and both key tests failed with *"cannot start a
transaction within a transaction"*, the exact corruption mechanism; restored
byte-for-byte and confirmed green. Three judgment calls, all sound:
(1) `approveBlueprintRevision`'s mid-try rollback was converted to a returned
boolean sentinel — correct, because its not-found branch only ever ran a
SELECT, so committing an empty transaction is identical to rolling one back,
and `null` is still returned without calling `getBlueprintRevision`;
(2) two PRE-EXISTING tests were rewritten (explicit `rollback()` → a throwing
body) because the raw triple is no longer callable — assertions unchanged, so
intent is preserved; (3) `scripts/mssql-probe.ts` was edited despite being
outside the plan's declared in-scope list — accepted, because Step 1 of the
plan itself mandates converting any caller found outside `platform-db.ts`, and
that script types against `SqlDriver`, so it cannot compile otherwise. Minor
nit not worth a revision: that script now uses `driver!` non-null assertions
inside the new closures. Merged to local main (fast-forward, `a487e33`).

**016 execution record (2026-07-22).** Docs-only, so the risk was wrong claims
rather than broken code — I re-verified every factual assertion against disk.
All criteria hold: `make verify` gone from `CLAUDE.md`, `web/src` count 0 across
AGENTS/DESIGN/VERIFICATION, no `Makefile` created, ADR range corrected, and a
loop over every backticked repo-relative path in `AGENTS.md` reports only
`tests/` — the illustrative per-workspace frozen-tests reference, not a repo
path. Spot-checked the new content: 9 routers named vs 9 on disk; ADRs
0001–0017 + 0021–0026 matching the "0018–0020 absent" note. The executor
improved on the plan twice: it found an **error in the plan itself** (I listed 7
kit components; there are 8 — `type-chip` — and it verified rather than copying
my list), and its `FACTORY_CLI` row states outright that the code default and
the cluster pin "disagree", which is franker than my suggested wording.
`task verify` was run green by the executor; I did not re-run it, having
confirmed that root-level `.md` files fall outside `format:check`'s path list
(`apps/*/src`, `packages/shared/src`), so these edits cannot affect the gate.
One open judgment call, accepted: `docs/IMPROVEMENTS.md` still says
`make verify` in a present-tense process description, but the loop it describes
is marked CONCLUDED — left as a historical record. If that loop is ever
resumed, that line needs fixing. Merged to local main (fast-forward, `5fa6c18`).

**019 execution record (2026-07-22).** Both parts landed; criteria pass; two
clean commits. **But the plan's premise was partly wrong, and the correction
matters more than the plan did.** I had asserted that pushing would newly
disclose both the CI password and the Azure hostname. Testing properly —
grepping `origin/main`'s tree rather than inferring from the diff range — the
truth is split: the **server FQDN was NOT on origin/main (0 files)**, so Part A
genuinely prevented a first-time disclosure; the **admin login (3 files) and the
CI password (2 files) were ALREADY public**, so templating them is hygiene, not
prevention. My earlier scan grepped whole files that had *changed* in the
unpushed range and conflated that with values *added* in it. Consequence: the
action that actually matters is Azure-side (the admin login name is public, so
password strength + firewall are the only controls), not repo-side. A third
file, `docs/superpowers/plans/2026-07-14-plan-a-db-backbone-azure-sql.md`, still
carries both and is already public. Nit not sent back: the URL-encoded password
in `FACTORY_DB_URL` is not `::add-mask::`-ed and differs from the masked literal
(`!` → `%21`); never printed, so theoretical. Part B is verified structurally
only — the `test-mssql` job runs on GitHub, never locally.

**017 execution record (2026-07-22, one revision round).** STOPPED on its first
run, correctly, and surfaced a **pre-existing landmine worth more than the bug
it was sent to fix**: `test_turn_order_migration_repairs_legacy_duplicates_before_indexing`
pins a temp DB to revision `c3e5a7b9d1f4` (`test_migrations.py:109`) and then
builds its legacy row from the **live ORM class** (:119). `Request` has 16
nullable non-JSON columns and all predate that pin; the nullable columns added
since are JSON-typed, which SQLAlchemy omits from an auto-INSERT. `reporter_email`
is the first plain-typed nullable column added since — so it was the first to
trip a trap that would have caught the next routine model change. The executor
refused two workarounds that would have gone green while destroying meaning
(retyping the column to JSON; advancing the pin past the migration under test),
and caught that the plan's suggested revision id `d4e6f8a0c2b4` **already exists**
in the repo. I widened the plan's scope to that one test and re-dispatched.
Verified after: 15 targeted tests, **879 passed / 4 skipped** full suite, single
alembic head, ruff clean, and my own falsification (reverting `message["To"]`
→ 2 failures). The test rewrite is properly bounded — 2 hunks inside the one
named test, **zero assertion lines changed**, duplicate `order` values (0,0,1 in
both child tables) preserved so the repair is still exercised. Committed as TWO
commits so the landmine fix is separable: `99e542b` (test_migrations only) and
`4679d4b` (the feature). **NOT merged** — blocked twice over: `api/app/routers/requests.py`
is dirty in the operator's tree (an in-flight interview refactor), so git refuses
a fast-forward; and `main` has since moved to `28dd78b`, making this a real merge
rather than a fast-forward. Both are the operator's call.

**Note for 018**: it adds `active_request_id` (Integer) to `Request` and will hit
the identical `test_migrations.py` wall. It needs `99e542b` first — cherry-pick
that commit ahead if 018 runs before 017 merges.

**Dispatch notes for 014–015** (learned executing 012 and 013, all environment, none a
plan defect): a fresh worktree may be created from an OLD commit — the first
attempt landed on `3b36cab`, 13 commits behind `main`, where `app-preview/`
does not exist at all; tell the executor to `git switch -c <branch> <sha>` onto
the plan's `Planned at` commit before anything else. A fresh worktree also has
no `node_modules` and no `.venv`, so a repo-root `npm ci` plus
`cd api && uv sync` are needed before `task verify` can run. And the system
Node may be newer than the pin (v25.6.1 vs `.nvmrc` 24.15.0) — `nvm use` is
required before any npm command.

- **012 first, always.** Until it lands, `task verify` goes green without
  running a single one of the ~12,000 lines of `app-preview/` tests — so
  013, 014 and 015 (all orchestrator changes) are not actually gated by
  anything. The dependency is soft only because those plans tell the executor
  to run `npm test` by hand.
- **013, 014, 015 all touch `app-preview/orchestrator/src`** but different
  files (`preview-manager.ts` + `kube-sandbox.ts` + `factory.ts` / 
  `chat-store.ts` / `sql-driver.ts` + `platform-db.ts`). The one overlap:
  013 makes `PreviewManager.dispose()` async, which means every
  `dispose()` call site in the test tree needs an `await` — 014's test file
  (`version-chat-lifecycle.test.ts`) is one of them. Run 013 before 014, or
  expect a trivial conflict.
- **016, 017, 018 are independent** of everything and of each other.
- **018 before enabling the ng-v0 bridge.** Both races it fixes are currently
  unreachable (`FACTORY_IMPORT_EDIT` is in no manifest). That is the reason to
  fix them now rather than after a user hits one. No plan here enables the
  flag — that stays an operator decision.

### Findings surfaced and NOT planned (operator deferred — do not re-audit)

Full evidence for each is in the audit report referenced by the session that
produced these plans. Recorded here so the next sweep does not re-derive them:

- **Untrusted-pod egress walls omit the node CIDR.** `agent-stage-walls`,
  `build-walls`, `sandbox-walls` and `registry-proxy-walls` in
  `deploy/base/networkpolicies.yaml` except the pod/service/link-local CIDRs
  but not the node subnet (`172.18.0.0/16`; node IP `172.18.0.2`, verified
  live). So a pod running LLM-authored code reaches kube-apiserver `:6443`,
  ingress-nginx `:80`, and the registry NodePort. HIGH confidence, S effort.
- **`POST /api/operators` is ungated and `role` defaults to `"admin"`**
  (`api/app/routers/operators.py:41`, `api/app/schemas.py:61`), and
  `_resolve_operator` (`api/app/auth.py:204`) never demotes a row for a token
  carrying no role claim — so under `FACTORY_AUTH=entra` a submitter can mint
  themselves admin. HIGH confidence, S effort.
- **The GitHub PAT and the codex credential are written inside the agent's own
  writable workspace** (`docker/sf-agent/entrypoint.sh:32-41`, `:136-141`).
  The code already strips the credentialed origin for the review stage only.
- **Attachment IDOR**: `GET /api/attachments/{aid}/raw`
  (`api/app/routers/attachments.py:50`) has no request scoping, while `delete`
  next door does.
- **Angular 22.0.0 advisories** — 5 high + 1 moderate on direct `@angular/*`
  deps (`npm audit --omit=dev`); a patch bump within 22.x fixes them.
- **`implementation-notes.md` is 64% duplicate** — the
  `## E2E-7: OpenShift Local (CRC)` block appears 16 times, byte-identical;
  `sort -u` takes it from 4,461 to 1,598 lines.
- **`outputs/chrome-bookmarks-current.md`** — a personal Chrome bookmarks
  export tracked in a public repo. Untracking does not remove it from history.
- **`.github/skills/impeccable/`** is a 96-file / 2.0 MB copy of
  `.claude/skills/impeccable/`, 16 files already drifted; the `.github` copy is
  the stale one.
- **29 git worktrees / 12 GB** under `.claude/worktrees/`; only 3 branches hold
  unmerged work. Repo-wide grep returns ~60× noise as a result.
- **`FACTORY_CLI` three-way drift**: code default `opencode`
  (`api/app/agent_exec.py:50`, ADR 0024), cluster configmap `codex`
  (`deploy/base/configmap.yaml:9`), `AGENTS.md:96` lists neither correctly.
  (Plan 016 fixes the *doc*; the config decision is the operator's.)
- **Five knobs bypass `settings.py`** via `__import__("os").environ` at
  `api/app/kube_runner.py:92-110`, including `FACTORY_GATE_INFRA_LIMIT` — the
  "three consecutive infra outcomes escalate" bound — which is therefore
  untestable via the repo's `monkeypatch.setattr(settings, …)` convention and
  absent from every manifest.
- **Design tokens duplicated across both `styles.css`** and already drifted on
  `--amber` and `--faint`; `packages/shared/` ships zero CSS.
- **`cost.py` read paths**: `fleet_cost` selects the entire `stage_jobs` table
  unfiltered; `_preview_slots_full` is an O(N²) query loop with a byte-for-byte
  duplicate at `kube_runner.py:1423`.
- **The tick loop does blocking SMTP and HTTP**, and 3 of its 8 drivers
  (`_reap_finished_previews`, `_sweep_preview_ttl`, `_monitor_app_health`) lack
  the per-driver try/except the other 5 have — so one bad row stalls every
  request that tick.
- **Log-scrub coverage gaps**: `api/app/log_scrub.py` cannot recognise a DB
  connection string (no URL-userinfo or ODBC `PWD=` pattern; `*_DB_URL` names
  do not match its secret-key regex), and three logging paths bypass scrubbing
  that neighbouring paths apply.
- **No `.env.example`** despite `.gitignore` reserving the name, for 120+ env
  vars.
- **`httpx` is declared dev-only** in `api/pyproject.toml` but imported at
  module load by `api/app/auth.py` and `api/app/github.py`; it survives the
  `--no-dev` image only transitively via `anthropic`.
- **Preview proxy**: `preview-bridge.ts:191-196` builds a `Response` with a body
  on the HTML branch without the null-body status guard its sibling branch has
  (a `304` + `text/html` throws → 502), and both paths `await
  upstream.arrayBuffer()` instead of piping, so a streaming response from a
  generated app never resolves.
- **`api/app/routers/requests.py` is 55% service layer** — the first `@router`
  decorator is at line 686; lines 98–683 are interview generation, a routing
  cache, two spawned threads, and three SSE pipelines.

### Direction findings (options for the operator, not defects)

- **Finish the ng-v0 bridge rollout.** Both halves, a migration, 363 lines of
  tests, and the intake UI are all built; `FACTORY_IMPORT_EDIT` is in zero
  manifests and `apps/intake/src/app/core/ngv0.config.ts:9-10` hardcodes
  `localhost:4200` / `127.0.0.1:7071`, so the button is dead in every
  deployment. Largest built-to-shipped gap in the repo.
- **Cut the orchestrator over to Azure SQL.** The recorded blocker ("a pod
  cannot `az login`") does not apply — `app-preview/orchestrator/src/sql-driver.ts:229-241`
  already does service-principal auth. Only the manifest is missing.
- **Give the orchestrator an identity model.** 31 routes, no auth anywhere, a
  mounted SA token that can create Deployments, plaintext connection secrets.
  This gates the two above rather than competing with them.
- **Close the GitHub loop inbound.** `api/app/github.py` is six outbound calls
  and no webhook receiver, while ADRs 0002/0005 promise PR-centric control and
  `protect_main` actively enables branch protection.
- **Make acceptance criteria enforce.** `api/app/settings.py:290-291` says it
  outright — "v1 only records structural evidence and never changes a gate
  verdict". The AST-level mapping is built and only emits an event.

## Dependency notes

- **003 before 004 and 006**: both produce large diffs; landing the lint
  gate first means those diffs are born lint-clean instead of needing a
  follow-up sweep. 004 can proceed without 003 if needed (soft dep).
- **005 before 006**: 006 moves the code 005 edits (`main.py` neighbors);
  doing them in this order avoids a pointless merge conflict.
- **007 last (recommended)**: AGENTS.md should describe the repo as it is
  after the other plans land (e.g. `make verify` including lint).
- 001 and 002 are independent of everything; do them first/anytime.

## Findings considered and rejected (do not re-audit)

From the 2026-06-11 audit (4 parallel auditors, every finding vetted against
the code before judgment):

- **Approve double-fire race** (`main.py` approve): already handled — the
  atomic `UPDATE … WHERE status='pending_approval'` claim means only the
  winner emits events; the loser takes the idempotent path. A test asserts
  single emission (`test_approve_ledger_and_idempotent_replay`).
- **"Subscription leak" in components** (`api.apps().subscribe` etc.):
  HttpClient observables complete after one emission; not a leak.
- **Stale `pending_question` after send-back**: the interview only runs in
  draft/submitted; after submission no UI or endpoint path re-reads it.
- **Docker `--host 0.0.0.0`**: required for nginx→api proxying inside the
  compose network; the api port is never published to the host.
- **N+1 / lazy-load claims on detail + comments endpoints**: each is a
  single bounded query (or ~4 lazy loads per detail fetch) — fine at this
  scale; `selectinload` is an optional micro-optimization, not a finding.
- **Duplicate-detection scan cost**: already status-gated to pre-approval
  and bounded to 200 rows (the code comment documents the earlier fix).
- **Simulator per-tick scan**: single indexed-ish query over a small table;
  ADR 0013 already addressed the real polling costs.
- **Ledger replay test gap**: the test already asserts `repo_ready`,
  `spec_pr_open`, `stage2_fired` and event-count stability on replay.
- **Claude-runner gate tests assert only escalation**: false — they assert
  the escalation REASON ("Test-isolation gate", "RED gate") and the rollback
  of the weakened test file.
- **Smoke.sh missing failure scenarios**: covered in pytest instead
  (`test_architecture.py`: orphan rescan, crashed stage, merge failure,
  cancel-beats-merge).
- **Subprocess env allow-list** (claude_exec): deferred deliberately — the
  CLI needs ambient auth; an allow-list buys little in a local tool and
  risks breaking `FACTORY_RUNNER=claude`. Revisit if deployed beyond
  localhost (noted in plan 005's out-of-scope).

## Direction findings (presented, not planned — product calls)

- **Surface reach/impact in admin triage**: fields are captured (intake) and
  in the spec ledger, but no admin view shows/sorts/filters them. Natural
  follow-up once the owner decides whether impact *informs* or *drives*
  priority.
- **Auto-suggest priority from reach/impact**: cheap after the above;
  needs the same product decision.
- **GitHub-real mode**: ADRs 0001/0002/0005 promise a PR-centric,
  GitHub-enforced factory; the Claude runner currently does local git only.
  A spike/design plan (not a build plan) would scope the gap honestly.
