apiVersion: v1
kind: Service
metadata:
  name: intake
spec:
  ports:
    - {name: http, port: 80, targetPort: 8080}
---
apiVersion: v1
kind: Service
metadata:
  name: console
spec:
  ports:
    - {name: http, port: 80, targetPort: 8080}
